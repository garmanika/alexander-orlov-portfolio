{
  "name": "orlov-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Freelance portfolio — System concept",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node tests/check.mjs",
    "test:browser": "node scripts/test-browser.mjs",
    "check": "node --check src/app.mjs && node --check src/projects.mjs && node --check scripts/serve.mjs && node --check scripts/build.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
